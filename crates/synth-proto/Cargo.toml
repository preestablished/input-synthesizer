[package]
name = "synth-proto"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
determinism-proto = { workspace = true, features = ["inputsynth"] }

[dev-dependencies]
prost = { workspace = true }
